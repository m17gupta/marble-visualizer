import UserProfileHome from "../components/userProfile/UserProfileHome";

const UserProfiles = () => {
  //  if (isAuthenticated && user !== null && user.id !== undefined) {
  //    dispatch(getUserProfile(user?.id));
  //  }
  return <UserProfileHome />;
};

export default UserProfiles;
