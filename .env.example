# AWS S3 Configuration for Direct Upload
# These are your S3 bucket details that will be used for direct uploads
# Note: In Vite, environment variables must be prefixed with VITE_ to be accessible in the browser

# AWS Region where your S3 bucket is located
VITE_AWS_REGION=us-east-1

# Your S3 bucket name
VITE_S3_BUCKET_NAME=your-bucket-name

# AWS Access Credentials (⚠️ SECURITY WARNING: These expose your AWS credentials to the client)
VITE_AWS_ACCESS_KEY_ID=your-access-key-id
VITE_AWS_SECRET_ACCESS_KEY=your-secret-access-key

# Optional: CloudFront distribution URL (if you're using CloudFront)
VITE_CLOUDFRONT_URL=https://your-cloudfront-domain.cloudfront.net

# Backend API URL (if using presigned URLs instead)
VITE_API_BASE_URL=http://localhost:3001/api
